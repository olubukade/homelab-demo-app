apiVersion: v1
kind: Service
metadata:
  name: homelab-demo-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: homelab-demo
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
